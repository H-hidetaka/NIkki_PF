<% content_for(:title, @diary.title) %>
<div class ="container">
  <div class="col-lg-8 offset-lg-2">
      <h1><%= t('.title') %></h1>
      <%= render 'form', { diary: @diary } %>
    </div>
  </div>
</div>
