<%= link_to bookmarks_path(diary_id: diary.id),
            id: "js-bookmark-button-for-diary-#{diary.id}", class:"float-right",
            method: :post,
            remote: true do %>
  <%= icon 'far', 'star' %>
<% end %>
