<%= link_to bookmark_path(current_user.bookmarks.find_by(diary_id: diary.id)),
          id: "js-bookmark-button-for-diary-#{diary.id}",
          class:"float-right",
          method: :delete,
          remote: true do %>
  <%= icon 'fas', 'star' %>
<% end %>
